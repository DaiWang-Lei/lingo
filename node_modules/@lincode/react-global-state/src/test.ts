import store, { rename } from "."

const [useTest, setTest, getTest] = store({ hello: "world", a: 1 })

getTest(val => console.log(val))

const renameTest = rename(setTest, getTest)

renameTest("hello", "ahoj")

console.log(getTest())